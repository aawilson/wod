defmodule WODTest do
  use ExUnit.Case, async: true

  test "can send \"today\" to get the wod for today"

  test "can send \"yesterday\" to get the wod for yesterday"

  test "can fetch from local storage if it exists"

  test "can fetch from the network if it doesn't exist in local storage"

  test "can restart (and erase local storage)"
end
